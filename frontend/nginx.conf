pid /tmp/nginx.pid;

events {}

http {
    include mime.types;
    default_type application/octet-stream;

    server {
        listen 8080;
        root /var/www;

        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}
